const usuario = {
    nombre: 'carlos',
    edad: 27,
    amigos: ['alejandra', 'cesar', 'manuel'],
    saludo: () => {
        console.log('hola');
    }
};

// metodos propios
// los objetos pueden tener metodos personalizados.
usuario.saludo();